apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bee-web.fullname" . }}-test-connection"
  labels:
    {{- include "bee-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bee-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
