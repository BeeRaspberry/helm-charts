apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bee-ui.fullname" . }}-test-connection"
  labels:
    {{- include "bee-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bee-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
