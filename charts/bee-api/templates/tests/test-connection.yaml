apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bee-api.fullname" . }}-test-connection"
  labels:
    {{- include "bee-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bee-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
